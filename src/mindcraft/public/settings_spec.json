{
    "profile": {
        "type": "object",
        "required": true,
        "description": "The profile object to use, including name, prompts, and examples"
    },
    "base_profile": {
        "type": "string",
        "description": "Allowed values: survival, assistant, creative, god_mode. Each has fine tuned settings for different game modes.",
        "default": "survival",
        "options": ["survival", "assistant", "creative", "god_mode"]
    },
    "load_memory": {
        "type": "boolean",
        "description": "Whether to load the bridge agent's previous memory",
        "default": false
    },
    "init_message": {
        "type": "string",
        "description": "The initial message to send to the bridge agent",
        "default": "Respond with hello world and your name"
    },
    "blocked_actions": {
        "type": "array",
        "description": "List of bridge commands or actions that are blocked.",
        "default": ["!checkBlueprint", "!checkBlueprintLevel", "!getBlueprint", "!getBlueprintLevel"]
    },
    "max_messages": {
        "type": "number",
        "description": "Maximum number of recent messages to keep in context for LLM",
        "default": 50
    },
    "auto_compact_memory": {
        "type": "boolean",
        "description": "Whether the agent should automatically compact memory when the history grows too large.",
        "default": true
    },
    "persona_preset": {
        "type": "string",
        "description": "Choose a persona preset to shape the agent's behavior and style.",
        "default": "miku_nakano",
        "options": ["miku_nakano", "default"]
    },
    "num_examples": {
        "type": "number",
        "description": "Number of examples to select to help prompt better LLM responses",
        "default": 2
    },
    "log_all_prompts": {
        "type": "boolean",
        "description": "Whether to log all prompts to file. Can be very verbose.",
        "default": false
    },
    "chat_ingame": {
        "type": "boolean",
        "description": "Whether to send the bridge agent's chat replies to in-game chat.",
        "default": true
    },
    "launch_mode": {
        "type": "string",
        "description": "Fabric Bridge runtime mode. fabric_ui keeps the normal Minecraft client UI; fabric_headless tunes the bridge agent for lower render overhead.",
        "default": "fabric_ui",
        "options": ["fabric_ui", "fabric_headless"]
    },
    "bridge_url": {
        "type": "string",
        "description": "URL of the Fabric Bridge Mod HTTP server.",
        "default": "http://localhost:8765"
    },
    "bridge_queue_enabled": {
        "type": "boolean",
        "description": "Enable sequential task queue in the Fabric bridge. Actions are queued and executed one at a time, advancing on Baritone completion signals. When disabled, all actions fire simultaneously (backward-compat).",
        "default": true
    },
    "bridge_structured_output": {
        "type": "boolean",
        "description": "When true in fabric runtime, requests structured JSON responses ({reply, actions[]}) so actions are executed from parsed data and command text is not leaked into chat output.",
        "default": true
    },
    "use_textual_topography": {
        "type": "boolean",
        "description": "Enable compact surface map generation for the LLM. Useful for planning navigation and building from terrain data without raw 3D state.",
        "default": false
    },
    "textual_topography_radius": {
        "type": "number",
        "description": "Radius around the player for topographic surface mapping when use_textual_topography is enabled.",
        "default": 8
    },
    "textual_topography_format": {
        "type": "string",
        "description": "Choose how the surface map is formatted in prompts: coordinate_list or grid.",
        "default": "coordinate_list",
        "options": ["coordinate_list", "grid"]
    },
    "bridge_chat_whitelist": {
        "type": "array",
        "description": "If non-empty, only accept in-game chat from these player names in bridge mode. Enter a JSON array of player names, e.g. [\"Alice\", \"Bob\"].",
        "default": []
    },
    "bridge_chat_blacklist": {
        "type": "array",
        "description": "If non-empty, reject in-game chat from these player names in bridge mode. Useful for blocking the bridge agent's own username or noisy players.",
        "default": []
    }
}
